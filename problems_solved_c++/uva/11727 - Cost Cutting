#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t,arr[4],c=1;
    scanf("%d",&t);
    while(t--)
    {
        for(int i=0;i<3;++i) scanf("%d",arr+i);

        sort(arr,arr+3);
        printf("Case %d: %d\n",c++,*(arr+1));
    }
    return 0;
}
