#include <bits/stdc++.h>

using namespace std;

int main()
{
    string s;
    int c = 1;
    while(getline(cin,s) && s != "*")
    {
        if(s == "Hajj")printf("Case %d: %s\n",c++,"Hajj-e-Akbar");
        else printf("Case %d: %s\n",c++,"Hajj-e-Asghar");
    }
    return 0;
}
