#include <bits/stdc++.h>

using namespace std;

int main()
{
    string a,b,c;
    cin >> a >> b >> c;
    string r = a + b;
    sort(r.begin(),r.end());
    sort(c.begin(),c.end());
    if(r==c) cout << "YES\n";
    else cout << "NO\n";
    return 0;
}
