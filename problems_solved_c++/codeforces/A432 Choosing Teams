#include <iostream>
using namespace std;

   int n,k,c;
    int cou=0;
int main()
{
    cin >> n >> k;
    for(int i=0;i<n;++i)
    {
        cin >> c;
          if( (5 - c) >= k)  ++cou;
    }
    cout << (cou/3);
    return 0;
}
